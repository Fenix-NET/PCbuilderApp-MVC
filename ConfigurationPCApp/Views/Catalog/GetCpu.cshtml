@using PcBuilderApp.Models.DTOs;
@model List<CpuDto>;

@{
    ViewData["Title"] = "CPU";
}


<div class="content-list">
    <div class="sort-container">
        <p>Сортировать по цене:</p>
        <button class="sort-button">Сначала дорогие</button>
    </div>
    @foreach (var item in Model)
    {

        <div class="product-cart">
            <img src="/images/CPU/@item.ImageDir" alt="photo cart" class="img-product">
            <div class="product-info">
                <a href="" class="product-cart-ref"><p class="product-cart-name">@item.Model</p></a>
                <p class="price">Цена: <b></b>@item.Price руб.</p>
            </div>
            <button class="add-basket">Добавить в корзину</button>
        </div>
  
    }
   
</div>

